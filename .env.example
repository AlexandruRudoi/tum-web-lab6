# Copy to .env and fill in the values before running docker-compose.prod.yml

# PostgreSQL password for the salon database
POSTGRES_PASSWORD=changeme

# JWT signing key — must be at least 32 chars, keep it secret
JWT_KEY=replace-with-a-long-random-secret-key-here
